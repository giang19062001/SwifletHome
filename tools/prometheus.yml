global:
  scrape_interval: 30s
  scrape_timeout: 20s

scrape_configs:
  - job_name: "SWIFTLET-PROMETHEUS"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["host.docker.internal:3502"]
