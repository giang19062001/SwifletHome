global:
  scrape_interval: 10s
  scrape_timeout: 90s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "SWIFTLET-PROMETHEUS"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["host.docker.internal:3502"]
