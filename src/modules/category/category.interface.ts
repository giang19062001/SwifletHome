export interface ICategory {
  seq: number;
  categoryCode: string;
  categoryName: string;
  isActive: 'Y' | 'N';
  createdAt: string;
  updatedAt: string;
  createdId: string;
  updatedId: string;
}
